const message = [
	'No setup is required for this sample connector.',
	'',
	"Run `deno task build`, then install this folder from Gety's Custom Connectors settings page.",
].join('\n');

console.log(message);
